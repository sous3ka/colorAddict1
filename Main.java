import javax.swing.*;

public class Main {
    public static void main(String[] args) {
        JFrame pagePrincipale = new JFrame();
        UserInterface.creerPage(pagePrincipale);
        UserInterface.pageAccueil(pagePrincipale);
    }
}
